interface:
    type: python
    approot: interface
    config:
        python_version: v3.2

spider:
    type: python-worker
    approot: spider
    config:
        python_version: v3.2

postgres:
    type: postgresql

redis:
    type: redis
